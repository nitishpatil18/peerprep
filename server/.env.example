# server config
PORT=4000
NODE_ENV=development

# comma-separated list of allowed origins
CLIENT_URL=http://localhost:5173

# secrets - generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
GOOGLE_CLIENT_ID=

# external services
MONGO_URI=
REDIS_URL=
